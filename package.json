{
  "name": "camscii",
  "version": "1.0.0",
  "description": "Camscii — live camera feed rendered as real-time ASCII art in the browser",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
